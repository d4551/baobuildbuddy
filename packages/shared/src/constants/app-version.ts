/**
 * Application semantic version shown in the client shell (sidebar footer, diagnostics).
 * Bump when cutting a release; keep aligned with published packages where applicable.
 */
export const APP_SEMVER = "0.1.0" as const;
