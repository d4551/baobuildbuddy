/**
 * Canonical score thresholds used for match scores, interview scores, and readiness.
 * Single source of truth for UI styling and server-side classification.
 */
export const SCORE_DEVELOPING_THRESHOLD = 40;
export const SCORE_WARNING_THRESHOLD = 60;
export const SCORE_PASS_THRESHOLD = 80;
