/**
 * Statistics and dashboard aggregation constants.
 */

/** Max automation runs fetched for stats aggregation. */
export const STATISTICS_AUTOMATION_RUNS_LIMIT: number = 100;

/** Skill count at which career progress coverage reaches 100%. */
export const STATISTICS_SKILL_COVERAGE_TARGET: number = 20;
