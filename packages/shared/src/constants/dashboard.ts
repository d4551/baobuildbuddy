/**
 * Dashboard UI constants. Single source of truth for dashboard timing and layout.
 */

/** Interval in ms for rotating hero motivational text on the dashboard. */
export const DASHBOARD_HERO_TEXT_ROTATE_INTERVAL_MS = 3_200;
