/**
 * Soft / secondary / accent button tokens (daisyUI 5).
 * Complements PRIMARY / OUTLINE / GHOST / LINK action families.
 */

/** Soft filled CTA (btn-soft) — tertiary-but-filled chips/prompts. */
export const SOFT_ACTION_CLASS = "btn btn-soft h-11 min-h-11";

/** Secondary solid CTA (non-primary page actions). */
export const SECONDARY_ACTION_CLASS = "btn btn-secondary h-11 min-h-11";

/** Dense secondary solid. */
export const SECONDARY_ACTION_DENSE_CLASS = "btn btn-secondary btn-sm min-h-11";

/** Accent solid (brand preview / marketing accents only). */
export const ACCENT_ACTION_CLASS = "btn btn-accent h-11 min-h-11 border-0";
