const errorPage = {
  errorPage: {
    title: "{brand} ran into an error",
    fallbackMessage: "Something went wrong.",
    statusLabel: "Status",
    backToDashboardButton: "Back to dashboard",
    resetButton: "Reset",
  },
} as const;

export default errorPage;
