const settingsseoDescription = {
  settings: {
    seoDescription:
      "Manage profile identity, AI providers, notification preferences, and automation defaults from one control plane.",
  },
} as const;

export default settingsseoDescription;
