export declare const config: {
    port: number;
    host: string;
    dbPath: string;
    logLevel: string;
    corsOrigins: string[];
    /** When true, skip API key auth (local dev only) */
    disableAuth: boolean;
};
